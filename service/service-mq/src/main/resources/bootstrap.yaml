server:
  port: 9002
spring:
  application:
    name: service-mq
  cloud:
    nacos:
      config:
        shared-configs:
          - data-id: common.yaml
            group: DEFAULT_GROUP
            refresh: true