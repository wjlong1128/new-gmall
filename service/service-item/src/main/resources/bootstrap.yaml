server:
  port: 8202
spring:
  profiles:
    active: dev
  application:
    name: service-item
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        shared-configs:
          - data-id: common.yaml
            group: DEFAULT_GROUP
            refresh: true